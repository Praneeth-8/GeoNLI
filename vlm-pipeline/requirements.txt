# Core deep learning
torch==2.4.1+cu121
torchvision==0.19.1+cu121
torchaudio==2.4.1+cu121

# Transformers ecosystem
transformers==4.46.3
accelerate>=0.34.0

huggingface-hub>=0.24.0
tokenizers>=0.19.0
safetensors>=0.4.0

# Qwen2-VL specific
qwen-vl-utils>=0.0.8

# 4-bit / 8-bit quantization
bitsandbytes>=0.43.0

# LoRA fine-tuning (for later)
peft>=0.12.0
trl>=0.11.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# Data / utilities
datasets>=2.20.0
sentencepiece>=0.2.0
einops>=0.7.0
timm>=1.0.0

# Evaluation metrics (BLEU for captioning)
nltk>=3.8.0
rouge-score>=0.1.2
evaluate>=0.4.0

# Experiment tracking
tqdm>=4.66.0
